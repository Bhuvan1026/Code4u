# cook your dish here

for i in range(int(input())):
    n,k=map(int,input().split())
        l=list(map(int,input().split()))
    count=0
    for i in l:
        if i>int(k):
            count+=1 
    print(count)
            
