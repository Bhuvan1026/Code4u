from itertools import combinations

class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        sub=[]
        for i in range(len(nums)+1):
            sub.extend(combinations(nums,i))
        return sub
        
