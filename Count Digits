#python

def countDigits(n: int) -> int:
    n1=[int(i) for i in str(n)]
    #n1=list(set(n1))
    
    count=0
    for i in range(len(n1)):
        if(n1[i]!=0):
            if(n%int(n1[i])==0):
                count=count+1
    

    return count
